import { Routes } from '@angular/router';

import { ClientesPageComponent } from './clientes-page.component';

export const CLIENTES_ROUTES: Routes = [{ path: '', component: ClientesPageComponent }];
